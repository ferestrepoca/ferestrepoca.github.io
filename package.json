{
  "name": "ferestrepoca-github-io",
  "private": true,
  "type": "module",
  "description": "Sitio personal — datos canónicos JSON + Astro",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "data:validate": "node scripts/validate-data.mjs",
    "test": "node --test scripts/tests/*.test.mjs",
    "harvest:theses": "node scripts/harvest-theses.mjs",
    "harvest:publications": "node scripts/harvest-publications.mjs",
    "dev": "npm run data:validate && npm run --prefix web dev",
    "build": "npm run data:validate && npm run --prefix web build",
    "preview": "npm run --prefix web preview"
  }
}
